<Window x:Class="facturas.FacturasRecibidasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Facturas recibidas (XML)" Height="600" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Facturas recibidas (desde XML)" FontSize="20" FontWeight="Bold" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <ListBox x:Name="LbArchivos" DisplayMemberPath="Display" Height="420" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                    <Button Content="Abrir factura" Click="BtnAbrirFactura_Click" Margin="5" />
                    <Button Content="Cargar desde archivo" Click="BtnCargarDesdeArchivo_Click" Margin="5" />
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="10">
                <TextBox x:Name="TxtVista" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="400" IsReadOnly="True" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cargar XML de ejemplo" Click="BtnCargarEjemplo_Click" Margin="5" />
            <Button Content="Generar INSERT" Click="BtnGenerarInsert_Click" Margin="5" />
            <Button Content="Subir a BD (configurar)" Click="BtnUploadToDb_Click" Margin="5" IsEnabled="False" />
            <Button Content="Cerrar" Click="BtnCerrar_Click" Margin="5" />
        </StackPanel>
    </Grid>
</Window>